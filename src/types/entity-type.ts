import { Section } from "./section-type";

type Entity = {
	id: number;
	name: string;
	// numAp: number;
	// numCl: number;
	// clAvg: number;
	// clMax: number;
	// timestamp: string;
	// sumCl: number;
	// zone: number;
	// url: string;
	// coordinate: string;
	// style: string;
	// pic: string;
	// secId: number;
	section?: Section;
};

export type { Entity };
